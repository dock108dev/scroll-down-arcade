# Local development
# Required: API key for backend requests (injected server-side via X-API-Key header)
SPORTS_DATA_API_KEY=your-api-key-here

# Optional: override backend URL for server-side fetches (default: https://sda.dock108.dev)
# SPORTS_API_INTERNAL_URL=http://localhost:8000
